{
  "name": "toy-rag",
  "version": "0.1.0",
  "private": true,
  "description": "Toy RAG CLI ingest surface",
  "bin": {
    "notesrag": "./bin/notesrag.js"
  },
  "scripts": {
    "test": "node --test",
    "test:e2e": "bash test/e2e/cli.e2e.sh",
    "debug": "echo foobar | bin/notesrag.js index --debug --stdin"
  },
  "engines": {
    "node": ">=24.13.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "ollama": "^0.6.3"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
